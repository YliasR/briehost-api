[defaults]
# Roles live at infra/ansible/roles/, not next to the playbook.
# Ansible auto-loads this file when ansible-playbook runs from the repo root
# (which is the worker's WorkingDirectory).
roles_path = infra/ansible/roles
inventory = infra/ansible/inventory/production.ini
host_key_checking = False
retry_files_enabled = False
