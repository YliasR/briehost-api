# Production inventory for briehost provisioning.
# Replace host details with the real Proxmox node + SSH user.

[proxmox]
pve ansible_host=proxmox.school.local ansible_user=root

[proxmox:vars]
ansible_python_interpreter=/usr/bin/python3
